appId: ${APP_ID}
---
- assertVisible:
    id: team-feed-view
- scrollUntilVisible:
    element:
      id: team-feed-entry-queued
    direction: DOWN
    timeout: 30000
    centerElement: true
- assertVisible:
    id: team-feed-entry-queued
- assertVisible: 'Participant reconnect check: option B is ready.'
- takeScreenshot: ${CAPTURE_DIR}/01-feed-queued
