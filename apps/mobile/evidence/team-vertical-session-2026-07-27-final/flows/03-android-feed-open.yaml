appId: ${APP_ID}
---
- openLink: crewnext://events/evt_local_team_day_2026
- extendedWaitUntil:
    visible:
      id: event-hub
    timeout: 30000
- tapOn:
    id: event-hub-tab-feed
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible:
      id: team-feed-view
    timeout: 30000
- assertVisible: SYNCHRONISIERT
- assertVisible:
    id: team-feed-input

