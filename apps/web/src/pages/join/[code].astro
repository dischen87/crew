---
// Closed Preview intentionally emits no invite route. Invite and download
// publication require their own gateway, privacy, device, and release evidence.
export function getStaticPaths() {
  return [];
}
---
