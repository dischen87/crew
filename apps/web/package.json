{
	"name": "@crew/web",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"check": "bun run build && bun scripts/check-preview.ts",
		"preview": "astro preview"
	},
	"dependencies": {
		"astro": "^5"
	}
}
